[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so deep links (e.g. the public /sign/:token page) resolve
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
